name: Batch Build and Publish Manylinux CANN Image

on:
  workflow_dispatch:
    inputs:
      tags:
        description: 'Comma-separated manylinux tags (leave empty to use all from build_manylinux_arg.json)'
        required: false
        type: string
      publish:
        description: 'Publish the image'
        default: false
        type: boolean
        required: true

concurrency:
  group: batch-manylinux
  cancel-in-progress: false

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate matrix
        id: set-matrix
        run: |
          if [ -n "${{ inputs.tags }}" ]; then
            TAGS="${{ inputs.tags }}"
          else
            TAGS=$(jq -r '.manylinux[].tags[]' build_manylinux_arg.json | paste -sd ',' -)
          fi
          MATRIX=$(echo "$TAGS" | tr ',' '\n' | sed '/^$/d' | jq -R . | jq -s .)
          {
            echo "matrix<<EOF"
            echo "{\"cann_tag\":$MATRIX}"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

  build:
    needs: prepare
    strategy:
      max-parallel: 2
      fail-fast: false
      matrix: ${{ fromJson(needs.prepare.outputs.matrix) }}
    uses: ./.github/workflows/build_and_push_manylinux.yml
    with:
      cann_tag: ${{ matrix.cann_tag }}
      publish: ${{ inputs.publish }}
    secrets: inherit